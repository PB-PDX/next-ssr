services:
  postgres:
    image: postgres:15
    restart: always
    environment:
      POSTGRES_USER: spoken
      POSTGRES_PASSWORD: password
      POSTGRES_DB: spoken_dev
    ports:
      - "5433:5432"  # Use port 5433 instead of 5432